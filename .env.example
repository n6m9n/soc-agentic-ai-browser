# TEMPLATE ONLY — do NOT put a real key here. This file IS committed/pushed.
# Copy it to ".env" in each assignment folder (assignment-3-intent-parser and
# assignment-4-langchain-agent) and put your key in THAT file. ".env" is
# git-ignored, so your key is never committed or pushed.
#
# Free Gemini key, no credit card: https://aistudio.google.com/apikey
GOOGLE_API_KEY=your-free-gemini-key-here

# Optional: override the model if gemini-2.5-flash is busy (503 errors)
# GEMINI_MODEL=gemini-2.0-flash
