# Copy to .env (git-ignored). Never commit real keys.

# Free Gemini key (no credit card): https://aistudio.google.com/apikey
GOOGLE_API_KEY=your-free-gemini-key-here

# Optional overrides
# GEMINI_MODEL=gemini-2.0-flash
# EMBED_MODEL=models/gemini-embedding-001

# Google OAuth (Phase 4+): path to the OAuth client secret downloaded from
# Google Cloud Console (git-ignored). See README for setup.
# GOOGLE_CREDENTIALS=credentials.json
